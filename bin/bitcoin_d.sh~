#!/bin/bash

SUBJECT="Bitcoin dealer down!"
EMAIL="tomaz.znideric@gmail.com"
if ! ps ax | grep -v grep | grep "scripts/dealing.py" > /dev/null
then
	echo "bitcoin_d is not running?" | mail -s "$SUBJECT" $EMAIL
fi
